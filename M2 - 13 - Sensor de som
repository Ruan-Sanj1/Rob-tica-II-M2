int pino_som = ;
int pino_LED = ;

boolean estado = LOW;

void setup() {
pinMode( );
pinMode( );
}

void loop() {
   int som = digitalRead(pino_som);
   if (som) {
      estado = !estado;
      digitalWrite();
      delay(300);
   }
}
